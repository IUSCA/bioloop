import{_ as o,c as s,o as t,ag as r}from"./chunks/framework.C9SxlbOG.js";const u=JSON.parse('{"title":"Node.js Metrics Documentation","description":"","frontmatter":{},"headers":[],"relativePath":"api/05-performance/nodejs_metrics.md","filePath":"api/05-performance/nodejs_metrics.md","lastUpdated":null}'),a={name:"api/05-performance/nodejs_metrics.md"};function i(n,e,c,d,l,p){return t(),s("div",null,e[0]||(e[0]=[r('<h1 id="node-js-metrics-documentation" tabindex="-1">Node.js Metrics Documentation <a class="header-anchor" href="#node-js-metrics-documentation" aria-label="Permalink to &quot;Node.js Metrics Documentation&quot;">​</a></h1><p>This document provides an overview of the performance metrics collected by the application.</p><h2 id="nodejs-gc-duration-seconds" tabindex="-1"><code>nodejs_gc_duration_seconds</code> <a class="header-anchor" href="#nodejs-gc-duration-seconds" aria-label="Permalink to &quot;`nodejs_gc_duration_seconds`&quot;">​</a></h2><p><strong>Type</strong>: Histogram<br><strong>Source</strong>: Derived from <code>perf_hooks.PerformanceObserver</code> (<a href="https://nodejs.org/api/perf_hooks.html#garbage-collection-gc-details" target="_blank" rel="noreferrer">Node.js Documentation</a>)</p><p><strong>Description</strong>:<br> Measures the duration of garbage collection (GC) events, categorized by type: <code>major</code>, <code>minor</code>, <code>incremental</code>, or <code>weakcb</code>.</p><hr><h2 id="nodejs-active-resources" tabindex="-1"><code>nodejs_active_resources</code> <a class="header-anchor" href="#nodejs-active-resources" aria-label="Permalink to &quot;`nodejs_active_resources`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>process.getActiveResourcesInfo()</code></p><p><strong>Description</strong>:<br> Tracks the number of active resources currently keeping the event loop alive.</p><ul><li><code>nodejs_active_resources</code>: Count of unique active resources.</li><li><code>nodejs_active_resources_total</code>: Total count of all active resources.</li></ul><p><strong>Usage</strong>:</p><ul><li>Monitor the number of active resources to identify potential event loop bottlenecks.</li><li>Set thresholds to alert when the number of active resources exceeds a defined limit.</li></ul><hr><h3 id="deprecated-metrics-nodejs-active-requests-and-nodejs-active-handles" tabindex="-1">Deprecated Metrics: <code>nodejs_active_requests</code> and <code>nodejs_active_handles</code> <a class="header-anchor" href="#deprecated-metrics-nodejs-active-requests-and-nodejs-active-handles" aria-label="Permalink to &quot;Deprecated Metrics: `nodejs_active_requests` and `nodejs_active_handles`&quot;">​</a></h3><p><strong>Status</strong>: Deprecated in Node.js v23 (<a href="https://nodejs.org/api/deprecations.html#DEP0161" target="_blank" rel="noreferrer">Deprecation Notice</a>)<br><strong>Replacement</strong>: Use <code>nodejs_active_resources</code> and <code>nodejs_active_resources_total</code>.</p><hr><h2 id="process-start-time-seconds" tabindex="-1"><code>process_start_time_seconds</code> <a class="header-anchor" href="#process-start-time-seconds" aria-label="Permalink to &quot;`process_start_time_seconds`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>process.uptime()</code></p><p><strong>Description</strong>:<br> Tracks the number of seconds since the Node.js process started.</p><hr><h2 id="process-open-fds" tabindex="-1"><code>process_open_fds</code> <a class="header-anchor" href="#process-open-fds" aria-label="Permalink to &quot;`process_open_fds`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>fs.readdirSync(&#39;/proc/self/fd&#39;).length - 1</code></p><p><strong>Description</strong>:<br> Monitors the number of open file descriptors used by the Node.js process.</p><p><strong>Usage</strong>:</p><ul><li>Identify potential file descriptor leaks or excessive file usage.</li><li>Compare with <code>process_max_fds</code> to ensure the process is not nearing the OS limit.</li></ul><p><strong>Caveats</strong>:</p><ul><li><code>readdirSync</code> is a synchronous operation and may block the event loop if there are many file descriptors.</li><li><code>/proc/self/fd</code> is in-memory (procfs), so it is faster than disk I/O but still incurs system call overhead.</li></ul><hr><h2 id="process-max-fds" tabindex="-1"><code>process_max_fds</code> <a class="header-anchor" href="#process-max-fds" aria-label="Permalink to &quot;`process_max_fds`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>/proc/self/limits</code></p><p><strong>Description</strong>:<br> Represents the maximum number of file descriptors the process can open, as configured by the OS.</p><p><strong>Usage</strong>:</p><ul><li>Compare <code>process_open_fds</code> with <code>process_max_fds</code> to detect if the process is nearing the limit.</li><li>Helps identify potential file descriptor management issues.</li></ul><hr><h2 id="process-cpu-seconds-total" tabindex="-1"><code>process_cpu_seconds_total</code> <a class="header-anchor" href="#process-cpu-seconds-total" aria-label="Permalink to &quot;`process_cpu_seconds_total`&quot;">​</a></h2><p><strong>Type</strong>: Counter<br><strong>Source</strong>: Derived from <code>@opentelemetry/api</code></p><p><strong>Description</strong>:<br> Tracks the total CPU time (user + system) consumed by the process.<br><strong>TODO</strong>: Add implementation details.</p><hr><h2 id="osmemoryheaplinux" tabindex="-1"><code>osMemoryHeapLinux</code> <a class="header-anchor" href="#osmemoryheaplinux" aria-label="Permalink to &quot;`osMemoryHeapLinux`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>/proc/self/status</code></p><p><strong>Metrics</strong>:</p><ul><li><code>process_resident_memory_bytes</code> (VmRSS): Physical memory in use. (real footprint in RAM).</li><li><code>process_virtual_memory_bytes</code> (VmSize): Total virtual memory allocated (includes swapped-out and unused portions).</li><li><code>nodejs_heap_size_total_bytes</code> (VmData): Heap memory size.</li></ul><p><strong>Usage</strong>:</p><ul><li>Monitor memory usage to detect potential memory leaks or excessive memory consumption.</li></ul><hr><h2 id="heapspacessizeandused" tabindex="-1"><code>heapSpacesSizeAndUsed</code> <a class="header-anchor" href="#heapspacessizeandused" aria-label="Permalink to &quot;`heapSpacesSizeAndUsed`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>v8.getHeapSpaceStatistics()</code></p><p><strong>Metrics</strong>:</p><ul><li><code>nodejs_heap_space_size_total_bytes</code>: Total size of each heap space.</li><li><code>nodejs_heap_space_size_used_bytes</code>: Used size of each heap space.</li><li><code>nodejs_heap_space_size_available_bytes</code>: Available size in each heap space.</li></ul><p><strong>Heap Spaces</strong>:</p><ul><li><code>new_space</code>: The new space is where new objects are allocated. It is a semi-space garbage collector.</li><li><code>old_space</code>: The old space is where long-lived objects are allocated. It is a mark-sweep garbage collector.</li><li><code>code_space</code>: The code space is where compiled code is stored.</li><li><code>map_space</code>: The map space is where object property maps are stored.</li><li><code>large_object_space</code>: The large object space is where large objects are allocated.</li></ul><p><strong>Usage</strong>:</p><ul><li>Monitor memory usage per heap space to identify memory bottlenecks or leaks.</li><li>Focus on <code>new_space</code> for early detection of memory issues.</li></ul><hr><h2 id="heapsizeandused" tabindex="-1"><code>heapSizeAndUsed</code> <a class="header-anchor" href="#heapsizeandused" aria-label="Permalink to &quot;`heapSizeAndUsed`&quot;">​</a></h2><p><strong>Type</strong>: Gauge<br><strong>Source</strong>: Derived from <code>process.memoryUsage()</code></p><p><strong>Metrics</strong>:</p><ul><li><code>nodejs_heap_size_total_bytes</code>: The total heap size allocated for the Node.js (V8) process.</li><li><code>nodejs_heap_size_used_bytes</code>: The amount of heap memory used by the Node.js (V8) process.</li><li><code>nodejs_external_memory_bytes</code>: refers to the memory usage of C++ objects bound to JavaScript objects managed by V8.</li></ul><p><strong>Usage</strong>:</p><ul><li>Monitor overall heap usage to detect memory leaks or excessive memory consumption.</li><li>Compare with <code>heapSpacesSizeAndUsed</code> for detailed heap space analysis.</li></ul><p><strong>Caveats</strong>:</p><ul><li><code>process.memoryUsage()</code> iterates over memory pages, which may block the event loop for large heaps.</li></ul><hr><p>Here&#39;s a refined and professional version of your documentation:</p><hr><h2 id="eventlooplag" tabindex="-1"><code>eventLoopLag</code> <a class="header-anchor" href="#eventlooplag" aria-label="Permalink to &quot;`eventLoopLag`&quot;">​</a></h2><p><strong>Type:</strong> Gauge</p><h4 id="metrics" tabindex="-1">Metrics <a class="header-anchor" href="#metrics" aria-label="Permalink to &quot;Metrics&quot;">​</a></h4><ul><li><code>nodejs_eventloop_lag_seconds</code></li><li><code>nodejs_eventloop_lag_min_seconds</code></li><li><code>nodejs_eventloop_lag_max_seconds</code></li><li><code>nodejs_eventloop_lag_mean_seconds</code></li><li><code>nodejs_eventloop_lag_stddev_seconds</code></li><li><code>nodejs_eventloop_lag_p50_seconds</code></li><li><code>nodejs_eventloop_lag_p90_seconds</code></li><li><code>nodejs_eventloop_lag_p95_seconds</code></li></ul><h4 id="description" tabindex="-1">Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;Description&quot;">​</a></h4><p><code>eventLoopLag</code> measures the delay between scheduling a timer (<code>setImmediate</code>) and its corresponding callback execution. This provides insight into how long the event loop is blocked by other operations, serving as an indicator of potential performance bottlenecks in a Node.js application.</p><h4 id="computation-of-nodejs-eventloop-lag-seconds" tabindex="-1">Computation of <code>nodejs_eventloop_lag_seconds</code> <a class="header-anchor" href="#computation-of-nodejs-eventloop-lag-seconds" aria-label="Permalink to &quot;Computation of `nodejs_eventloop_lag_seconds`&quot;">​</a></h4><p>The <code>nodejs_eventloop_lag_seconds</code> metric is computed as follows <a href="https://github.com/siimon/prom-client/issues/561" target="_blank" rel="noreferrer">(reference)</a>:</p><ol><li>A <code>Gauge</code> is created in <code>eventLoopLag.js</code> with a custom <code>collect</code> method.</li><li>When metrics are requested, the <code>collect</code> method is invoked, capturing a high-resolution timestamp. This occurs at the start of the metrics collection process.</li><li>The method then schedules another measurement using <code>setImmediate</code>. Since this executes in the next event loop iteration, it occurs after metrics collection and reporting have completed.</li><li>As the <code>collect</code> method does not return a promise, the computed value is only recorded in the subsequent metrics collection cycle.</li></ol><p>This means the metric measures the delay from the start of metrics collection until the next event loop iteration. However, it is not updated in real time and only reflects values from the previous collection cycle.</p><h4 id="computation-of-other-metrics" tabindex="-1">Computation of Other Metrics <a class="header-anchor" href="#computation-of-other-metrics" aria-label="Permalink to &quot;Computation of Other Metrics&quot;">​</a></h4><p>Other event loop lag metrics (e.g., min, max, mean, percentiles) are derived from <code>perf_hooks.monitorEventLoopDelay()</code>.</p><ul><li>The minimum measurable delay depends on the timer resolution, which defaults to <strong>10ms</strong> but can be adjusted using the <code>eventLoopMonitoringPrecision</code> configuration option.</li></ul><h4 id="use-cases" tabindex="-1">Use Cases <a class="header-anchor" href="#use-cases" aria-label="Permalink to &quot;Use Cases&quot;">​</a></h4><ul><li><strong>Performance Monitoring</strong>: Helps detect high event loop lag, which may indicate that the application is being blocked by long-running operations.</li><li><strong>Bottleneck Identification</strong>: A consistently high event loop lag suggests potential issues such as synchronous operations blocking the event loop.</li></ul><h4 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h4><ul><li><strong>Delayed Updates</strong>: <ul><li><code>nodejs_eventloop_lag_seconds</code> is only updated during metrics collection. The reported value reflects the lag from the previous cycle, not the current one.</li><li>In high-throughput applications (e.g., Express-based servers), event loop lag typically increases in short bursts (e.g., during sudden spikes in requests). If metrics are collected every <strong>30 seconds</strong>, the reported value may not accurately represent real-time lag.</li></ul></li><li><strong>Resolution Constraints</strong>: <ul><li>Metrics derived from <code>perf_hooks.monitorEventLoopDelay()</code> have a <strong>10ms resolution</strong> by default. While this provides continuous monitoring, it may not capture sub-millisecond variations in event loop lag.</li></ul></li></ul>',82)]))}const m=o(a,[["render",i]]);export{u as __pageData,m as default};
