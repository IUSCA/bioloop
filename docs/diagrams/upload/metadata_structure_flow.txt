flowchart TD
    M[metadata JSONB]
    C[checksum\nmanifest_hash OR skipped flags]
    S[size_manifest\nmode,file_count,total_size,files_list]
    W[worker metadata\nverification_task_id,failure_reason,...]

    M --> C
    M --> S
    M --> W
