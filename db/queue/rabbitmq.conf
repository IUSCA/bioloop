# Celery 5.2 and its Kombu dependency use transient, non-exclusive queues for
# worker control replies. RabbitMQ 4.3 denies these queues by default. Keep the
# compatibility feature enabled until the worker stack can be upgraded to a
# version that uses durable or exclusive temporary queues.
deprecated_features.permit.transient_nonexcl_queues = true
