PORTAL_LAUNCH_URL=https://localhost/launch-notebook
JUPYTERHUB_COOKIE_SECRET=example
NOTEBOOK_IMAGE=research-notebook:latest
HUB_API_TOKEN=example
POSTGRES_PASSWORD=example
HUB_API_URL=http://localhost:8000/hub/api

# Shared secret between Hub and portal backend for HMAC-signed launch tickets.
# Used by jupyterhub_config.py (Hub side) and hub_api.py (portal side).
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
PORTAL_TICKET_SECRET=example


DATA_BASE_PATH=/path/to/data