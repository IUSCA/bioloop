import GrantCreated from "./GrantCreated.vue";
import GrantExpired from "./GrantExpired.vue";
import GrantRevoked from "./GrantRevoked.vue";

export default {
  GRANT_CREATED: GrantCreated,
  GRANT_REVOKED: GrantRevoked,
  GRANT_EXPIRED: GrantExpired,
};
