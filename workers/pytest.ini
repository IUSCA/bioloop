[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# tests/register_ondemand holds standalone scripts, not pytest tests: they define
# run_test() rather than test_*(), and they create and delete directories at import
# time. Collecting them runs those side effects and finds no tests. See the README
# in that directory for how to run them.
norecursedirs = register_ondemand

# Output
addopts =
    --verbose
    --strict-markers
    --tb=short
    --disable-warnings

# Markers
markers =
    integration: Integration tests (run against Docker services)
    slow: Slow tests (may take minutes)
    requires_celery: Tests that require Celery worker running
    watch_script: Tests for watch.py Observer/Register behavior
    integrated_wf: Tests for the integrated workflow steps (archive, validate, etc.)
    stage_wf: Tests for the stage workflow steps

# Logging. /tmp works both in the worker container and on a developer machine.
# To disable persistent log: --log-file=/dev/null
log_cli = true
log_cli_level = INFO
log_file = /tmp/bioloop_test_logs/watch_tests.log
log_file_mode = a
log_file_level = DEBUG
log_format = %(asctime)s %(levelname)-8s %(name)s:%(filename)s:%(lineno)d %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

# Test timeout: 10 minutes max per test (for slow tests)
timeout = 600
